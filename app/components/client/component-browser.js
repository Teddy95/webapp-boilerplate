// This ist the client javascript file. It has to be a own component beacause vite.js don't recognize the client.js import directly in template.marko

import '../../scripts/i18n'
import * as bootstrap from 'bootstrap'

window.bootstrap = bootstrap

export default {}