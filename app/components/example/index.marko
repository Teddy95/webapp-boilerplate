class {
	onCreate () {
		this.state = {
			count: 0
		}
	}

	increment () {
		this.state.count++
	}
}

style {
	.count {
		color: #ff9500;
		font-size: 3em;
	}
}

div.count
	-- ${state.count}

button.btn.btn-primary on-click('increment')
	-- Click me! <i.fas.fa-rocket></i>
