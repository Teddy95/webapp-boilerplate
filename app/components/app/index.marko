/**
 * Autor: Andre Sieverding
 * Copyright © 2019
 */

class {
	onCreate (input, out) {
		var view = require(`../../view/${out.global.view}`)

		this.state = {
			view: view
		}
	}
}

${state.view}
